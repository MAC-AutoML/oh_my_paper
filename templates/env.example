# Example secrets for future real API/App Server integration.
# Copy to .env for local use. .env is gitignored.
OPENAI_API_KEY=
CODEX_API_KEY=
GITHUB_TOKEN=
s2_api_key=

# Optional OpenAI-compatible model endpoint.
# The base URL is intentionally explicit because some users rely on this relay.
OPENAI_BASE_URL=https://automl.aiserverai.online/v1
OPENAI_MODEL=
OPENAI_REVIEWER_MODEL=
